# 웹툰 별점표 — 설치 가이드

순서대로 따라가면 된다. 1~4번까지 하면 사이트가 뜨고 품평을 쓸 수 있다.
5번(Supabase)은 댓글과 즐겨찾기용이라 나중에 해도 되고, 6번은 화수 자동 갱신이다.

메뉴 이름은 GitHub나 Supabase가 개편하면서 조금씩 바뀔 수 있다.
글자가 정확히 안 맞으면 비슷한 걸 찾으면 된다.

---

# 1. 리포지토리 만들고 파일 올리기

### 1-1. 계정
github.com에 들어가서 계정이 없으면 만든다. 무료 계정으로 충분하다.

### 1-2. 리포지토리 생성
1. 오른쪽 위 **＋** 를 누르고 **New repository**
2. **Repository name** 에 `webtoon-stars`
3. **Public** 을 고른다 (Private이면 Pages가 유료다)
4. 아래 체크박스는 전부 그대로 두고 **Create repository**

### 1-3. 파일 올리기
1. 압축 파일(`webtoon-stars_v1.zip`)을 푼다
2. 방금 만든 리포 화면에서 **Add file** → **Upload files**
3. 푼 폴더 **안의 내용물**을 전부 끌어다 놓는다
   - `index.html`, `admin.html`, `config.js`, `README.md`, `.gitignore`
   - `data` 폴더, `covers` 폴더, `scripts` 폴더
   - 폴더 자체를 통째로 끌면 한 단계 더 들어가버리니, 폴더를 열고 그 안의 것들을 끈다
4. 아래 **Commit changes**

### 1-4. .github 폴더는 따로 만든다
점으로 시작하는 폴더라 컴퓨터에서 숨겨져 있어 위에서 안 올라갔을 가능성이 크다.
리포 파일 목록에 `.github`가 없으면 이렇게 만든다.

1. **Add file** → **Create new file**
2. 파일 이름 칸에 이걸 그대로 친다 (슬래시까지)
   ```
   .github/workflows/weekly.yml
   ```
   슬래시를 칠 때마다 폴더가 자동으로 생긴다
3. 아래 큰 칸에, 압축 안의 `.github/workflows/weekly.yml` 내용을 복사해 붙여넣는다
4. **Commit changes**

---

# 2. config.js 고치기

1. 리포 파일 목록에서 **config.js** 를 누른다
2. 오른쪽 위 **연필 아이콘**(Edit this file)
3. 맨 위 `repo` 부분을 고친다

```js
repo: {
  owner:  "여기에-깃허브아이디",   ← 본인 GitHub 아이디
  name:   "webtoon-stars",       ← 리포 이름 (다르게 만들었으면 그 이름)
  branch: "main",
},
```

아이디는 리포 주소에 있다. `github.com/hyoje/webtoon-stars` 면 `hyoje`가 아이디다.

4. 오른쪽 위 **Commit changes** → 다시 **Commit changes**

`admins`와 `supabase`는 지금 안 건드려도 된다.

---

# 3. 사이트 켜기 (GitHub Pages)

1. 리포 위쪽 탭 중 **Settings** (톱니바퀴)
2. 왼쪽 목록에서 **Pages**
3. **Build and deployment** 아래 **Source** 를 **Deploy from a branch** 로
4. 그 밑 **Branch** 를 `main`, 오른쪽 폴더는 `/ (root)`
5. **Save**

1~2분 기다리면 같은 화면 위쪽에 주소가 뜬다.

```
https://내아이디.github.io/webtoon-stars/
```

들어가보면 사이트가 나온다. 표지가 색깔 판으로 보이는 게 정상이다.
아직 이미지가 없어서 제목으로 만든 자리표시자가 뜨는 것.

관리자 화면은 뒤에 `admin.html`을 붙인다.
```
https://내아이디.github.io/webtoon-stars/admin.html
```

---

# 4. 관리자 토큰 만들기

토큰은 "내가 이 리포에 글을 쓸 수 있다"는 증명서다. 사람마다 하나씩 만든다.

### 4-1. 토큰 만들기
1. 오른쪽 위 **프로필 사진** → **Settings**
   (리포 Settings 말고, 계정 Settings다)
2. 왼쪽 목록 **맨 아래**로 내려서 **Developer settings**
3. **Personal access tokens** → **Fine-grained tokens**
4. 오른쪽 위 **Generate new token**
5. 채운다
   - **Token name**: `별점표` (아무거나)
   - **Expiration**: `Custom` 으로 1년 뒤 정도. 짧게 하면 그때 다시 만들어야 한다
   - **Repository access**: **Only select repositories** 고르고, 아래 칸에서 `webtoon-stars` 선택
6. 아래 **Permissions** → **Repository permissions** 를 편다
   - 목록에서 **Contents** 를 찾아 오른쪽 드롭다운을 **Read and write** 로
   - 나머지는 전부 그대로 (Metadata는 자동으로 Read-only가 된다)
7. 맨 아래 **Generate token**
8. **화면에 뜬 토큰을 바로 복사한다.** 이 화면을 벗어나면 다시 못 본다.
   잃어버렸으면 그냥 새로 만들면 된다.

### 4-2. 관리자 화면 들어가기
1. `https://내아이디.github.io/webtoon-stars/admin.html` 로 간다
2. **나는 누구?** 에서 본인 이름을 고른다 (HJ / ys / sj)
3. 토큰을 붙여넣고 **들어가기**

들어가지면 성공. 왼쪽에 웹툰 14편이 뜬다.
토큰은 그 브라우저에만 저장돼서, 다음부터는 바로 들어가진다.

### 4-3. 안 들어가질 때
- **"리포지토리를 못 찾았다"** → `config.js`의 `owner`나 `name`이 틀렸다. 2번으로 돌아간다
- **"토큰이 안 먹는다"** → 4-1의 6번, Contents를 Read and write로 안 했을 가능성이 크다
- 토큰 앞뒤에 공백이 딸려왔을 수도 있으니 다시 붙여넣어 본다

---

# 5. 관리자 추가하기

새 사람을 관리자로 넣는 방법. 세 가지를 해야 한다.

### 5-1. config.js에 이름 넣기
1. 리포에서 **config.js** → 연필 아이콘
2. `admins` 부분을 찾아 줄을 하나 추가한다

```js
admins: [
  { id: "HJ", name: "HJ", color: "#f5b93a" },
  { id: "ys", name: "ys", color: "#6ee7b7" },
  { id: "sj", name: "sj", color: "#93c5fd" },
  { id: "jw", name: "재우", color: "#f9a8d4" },   ← 이렇게 한 줄 추가
],
```

- `id` 는 데이터에 저장되는 이름이다. 짧게, 영어로. **한 번 정하면 바꾸지 않는 게 좋다**
  (이미 쓴 품평이 이 id로 붙어 있어서, 바꾸면 그 사람 품평이 목록에서 사라진다)
- `name` 은 화면에 보이는 이름. 한글도 된다
- `color` 는 뷰어 화면에서 글쓴이 표시에 쓰는 색. 겹치지만 않으면 아무거나
- 쉼표 빠뜨리지 않게 주의

3. **Commit changes**

### 5-2. 리포에 초대하기
1. 리포 **Settings** (톱니바퀴)
2. 왼쪽 **Collaborators** (또는 **Collaborators and teams**)
3. **Add people**
4. 상대방 GitHub 아이디나 이메일을 넣고 선택
5. 권한은 **Write** 로 (Admin까지 줄 필요 없다)
6. **Add**

상대방한테 초대 메일이 간다. 수락해야 쓸 수 있다.

### 5-3. 그 사람이 자기 토큰 만들기
초대받은 사람이 **자기 계정으로** 4-1을 그대로 한다.
그리고 `admin.html`에서 자기 이름을 골라 자기 토큰을 넣는다.

토큰은 절대 서로 공유하지 않는다. 각자 만드는 게 맞다.

### 관리자 빼기
1. `config.js`의 `admins`에서 그 줄을 지운다
2. Settings → Collaborators에서 그 사람 옆 **Remove**

이미 쓴 품평은 남는다. 품평까지 지우려면 관리자 화면에서 하나씩 지워야 한다.

---

# 6. Supabase 붙이기 — 댓글과 즐겨찾기

이걸 해야 뷰어가 로그인해서 댓글을 달고 즐겨찾기를 할 수 있다.
안 하면 즐겨찾기는 기기별로만 저장되고 댓글은 아예 안 뜬다.

시간은 20~30분 정도 걸린다. Google 로그인 붙이는 게 제일 번거롭다.

## 6-1. 프로젝트 만들기

1. [supabase.com](https://supabase.com) → 오른쪽 위 **Start your project**
2. **Continue with GitHub** 로 로그인 (계정을 따로 안 만들어도 된다)
3. **New project**
   - **Name**: `webtoon-stars`
   - **Database Password**: 아무거나 길게. **어딘가 적어둔다** (나중에 필요할 수 있다)
   - **Region**: `Northeast Asia (Seoul)` — 한국에서 제일 빠르다
   - **Plan**: Free
4. **Create new project**

2분 정도 준비 시간이 걸린다. 기다린다.

## 6-2. 표 만들기

1. 왼쪽 목록에서 **SQL Editor** (종이에 `>_` 표시된 아이콘)
2. **New query** 또는 가운데 빈 칸을 누른다
3. 아래를 **전부** 복사해서 붙여넣는다

```sql
create table comments (
  id         bigint generated always as identity primary key,
  webtoon_id text        not null,
  user_id    uuid        not null,
  user_name  text        not null,
  body       text        not null,
  created_at timestamptz default now()
);

create table favorites (
  user_id    uuid not null,
  webtoon_id text not null,
  primary key (user_id, webtoon_id)
);

alter table comments  enable row level security;
alter table favorites enable row level security;

create policy "댓글 읽기" on comments for select using (true);

create policy "댓글 쓰기" on comments for insert
  to authenticated with check (auth.uid() = user_id);

create policy "댓글 지우기" on comments for delete
  to authenticated using (auth.uid() = user_id);

create policy "즐겨찾기" on favorites for all
  to authenticated using (auth.uid() = user_id)
  with check (auth.uid() = user_id);
```

4. 오른쪽 아래 **Run** (또는 `Ctrl+Enter`)
5. `Success. No rows returned` 가 뜨면 됐다

왼쪽 **Table Editor** 에 들어가면 `comments`와 `favorites` 두 개가 보인다.

> 아래쪽 `create policy` 부분이 중요하다. 이게 없으면 아무나 남의 댓글을
> 지울 수 있게 된다. 키가 공개돼도 괜찮은 이유가 이 규칙들 때문이다.

## 6-3. Google 로그인 붙이기

두 사이트를 오가야 한다. 순서대로 하면 헷갈리지 않는다.

### ① Supabase에서 콜백 주소 복사
1. 왼쪽 목록 **Authentication** (사람 아이콘)
2. **Sign In / Providers** (또는 **Providers**)
3. 목록에서 **Google** 을 누른다
4. 펼쳐지면 아래쪽에 **Callback URL (for OAuth)** 이 있다.
   `https://무슨무슨문자.supabase.co/auth/v1/callback` 이렇게 생겼다.
   **이걸 복사해둔다.** 이 탭은 켜둔 채로 다음으로 간다

### ② Google Cloud Console에서 열쇠 만들기
1. 새 탭에서 [console.cloud.google.com](https://console.cloud.google.com) 로 간다
2. 위쪽 프로젝트 선택 칸을 누르고 **새 프로젝트** → 이름 `별점표` → **만들기**
3. 만들어지면 그 프로젝트로 전환한다
4. 왼쪽 메뉴(햄버거) → **API 및 서비스** → **OAuth 동의 화면**
   (최근에 **Google Auth Platform** 으로 이름이 바뀌었을 수 있다)
   - **User Type**: **외부(External)** → 만들기
   - 앱 이름: `웹툰 별점표`
   - 사용자 지원 이메일: 본인 메일
   - 개발자 연락처 정보: 본인 메일
   - 나머지는 넘기고 저장
5. 왼쪽 **사용자 인증 정보(Credentials)** → 위쪽 **＋ 사용자 인증 정보 만들기**
   → **OAuth 클라이언트 ID**
   - **애플리케이션 유형**: **웹 애플리케이션**
   - **이름**: `별점표`
   - **승인된 리디렉션 URI** → **＋ URI 추가** →
     아까 복사한 **Supabase 콜백 주소**를 붙여넣는다
   - **만들기**
6. 창이 뜨면서 **클라이언트 ID** 와 **클라이언트 보안 비밀번호** 가 나온다.
   둘 다 복사해둔다

### ③ Supabase로 돌아와 붙여넣기
1. 아까 켜둔 Google 설정 화면으로 돌아간다
2. 맨 위 스위치를 켠다 (**Enable Sign in with Google**)
3. **Client ID** 와 **Client Secret** 에 방금 복사한 걸 넣는다
4. **Save**

### ④ 사이트 주소 등록
1. 왼쪽 **Authentication** → **URL Configuration**
2. **Site URL** 에 사이트 주소를 넣는다
   ```
   https://내아이디.github.io/webtoon-stars/
   ```
3. **Redirect URLs** 에도 같은 주소를 추가한다 (**Add URL**)
4. **Save**

## 6-4. 키를 config.js에 넣기

1. Supabase 왼쪽 아래 **Project Settings** (톱니바퀴)
2. **API** (또는 **API Keys** / **Data API**)
3. 두 개를 복사한다
   - **Project URL** — `https://무슨문자.supabase.co`
   - **anon** / **public** 키 — `eyJ...` 로 시작하는 긴 문자열
     (**service_role** 키는 절대 쓰면 안 된다. 그건 진짜 위험한 키다)
4. GitHub 리포에서 **config.js** → 연필 아이콘
5. 아래처럼 채운다

```js
supabase: {
  url:     "https://무슨문자.supabase.co",
  anonKey: "eyJhbGciOi...아주긴문자열",
},
```

6. **Commit changes**

## 6-5. 확인

1~2분 뒤 사이트에 들어가서 오른쪽 위 **구글로 로그인** 을 눌러본다.
구글 창이 뜨고, 로그인하면 사이트로 돌아온다.

웹툰 하나를 눌러 상세를 열고 아래 댓글 칸에 뭐라도 써본다.
Supabase의 **Table Editor → comments** 에 들어가면 방금 쓴 게 보인다.

### 안 될 때
- **구글 창에서 "redirect_uri_mismatch"** → 6-3 ②의 리디렉션 URI가
  Supabase 콜백 주소와 정확히 안 맞는다. 끝의 슬래시까지 똑같아야 한다
- **로그인 후 엉뚱한 데로 감** → 6-3 ④의 Site URL을 확인한다
- **댓글이 안 올라감** → 6-2의 `create policy` 를 안 돌렸을 가능성.
  SQL Editor에서 policy 부분만 다시 실행해본다
- **아무 반응 없음** → 브라우저에서 `F12` → **Console** 탭에 빨간 글씨가 있는지 본다

---

# 7. 화수 자동 갱신 켜기

1. 리포 위쪽 탭 **Actions**
2. 처음이면 "Workflows aren't being run on this forked repository" 같은
   안내와 함께 초록 버튼이 뜬다. 누른다
3. 왼쪽에 **주간 화수 갱신** 이 보이면 켜진 거다

바로 시험해보려면
1. 왼쪽에서 **주간 화수 갱신** 을 누른다
2. 오른쪽 **Run workflow** → 다시 **Run workflow**
3. 1분쯤 뒤 새로고침하면 결과가 뜬다. 눌러 들어가면 어느 작품이
   몇 화에서 몇 화로 바뀌었는지 로그가 나온다

기본은 매주 일요일 새벽 4시(한국시간)에 자동으로 돈다.

**대상은 관리자 화면에서 "자동 갱신"을 켜고 주소를 넣어둔 작품뿐이다.**
지금 데이터에서는 전지적 독자 시점 하나만 켜져 있다.

**지금은 네이버웹툰만 읽는다.** 카카오·레진·탑툰은 자바스크립트로 화면을
그려서 이 방식으로 안 읽힌다. 나중에 붙이려면
`scripts/update-episodes.mjs` 의 `PARSERS` 에 함수를 추가하면 된다.

휴재하면 화수가 그냥 안 늘어난다. 읽기에 실패하면 관리자 화면 아래에
"확인 실패"로 표시되고 숫자는 안 건드린다.

---

# 정리 — 어디서 뭘 하나

| 하고 싶은 것 | 가는 곳 |
|---|---|
| 품평 쓰기 | `사이트주소/admin.html` |
| 관리자 추가 | `config.js`의 `admins` + 리포 Settings → Collaborators |
| 장르 추가 | `config.js`의 `genres` |
| 플랫폼 추가 | `config.js`의 `platforms` |
| 신작 기준 바꾸기 | `config.js`의 `newDays` (지금 183일) |
| 댓글 지우기 (남의 것) | Supabase → Table Editor → comments |
| 잘못 저장한 거 되돌리기 | 리포 → 커밋 기록에서 해당 커밋 → Revert |
| 화수 지금 바로 갱신 | Actions → 주간 화수 갱신 → Run workflow |
